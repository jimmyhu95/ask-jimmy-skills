---
name: grill-me
description: A relentless interview to sharpen a plan or design.
disable-model-invocation: true
---

有代码库时优先 `/grill-with-docs`。决策清楚后走 `/ask-jimmy`：跨会话才 `/to-spec`；≥5 个可独立验收切片才 `/to-tickets`。主 agent 写计划，默认用 `subagent-driven-development`。

Run a `/grilling` session.
